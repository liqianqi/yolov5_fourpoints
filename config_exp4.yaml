# exp4 模型最优推理配置
# 基于验证集测试结果优化

# 模型路径
weights: runs/train/exp4/weights/best.pt

# 推理参数
img_size: 640
conf_thres: 0.4 # 置信度阈值 (平衡精度和召回)
iou_thres: 0.3 # NMS IOU阈值 (降低后mAP@0.5从0.309提升到0.326)
max_det: 50 # 每张图最大检测数
device: 0 # GPU设备

# 验证集性能 (iou_thres=0.3)
# Precision: 0.494
# Recall: 0.420
# mAP@0.5: 0.326
# mAP@0.5:0.95: 0.136

# 数据集信息
data: data/data36.yaml
nc: 36 # 类别数
